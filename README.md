# File-Organizer-Python-
The Python File Organizer is a simple automation tool that organizes files in a selected folder based on their file types. It automatically scans the directory, creates folders for different file extensions (like Images, Documents, Videos, etc.), and moves files into their respective folders.

## 📌 Overview
Python File Organizer is a simple automation script that organizes files in a folder based on their file types. It automatically creates folders for different file extensions and moves files into them.

This helps keep your directories clean and well-structured.

---

## 🎯 Features
- Automatically detects file extensions
- Creates folders by category
- Moves files into respective folders
- Prevents overwriting duplicate files
- Easy to use

---

## 🛠 Technologies Used
- Python 3
- os module
- shutil module

---

## 📂 Project Structure

